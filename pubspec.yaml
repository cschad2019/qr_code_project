name: qr_code_project
description: A Flutter project for generating customizable QR codes.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  qr_flutter: ^4.0.0  # Package for QR code generation

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true


